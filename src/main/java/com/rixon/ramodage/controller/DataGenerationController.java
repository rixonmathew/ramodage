package com.rixon.ramodage.controller;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class DataGenerationController {

    public void generateData() {
        /*
          Load the options, read from request, generate data and provide response
          For async mode return a request id and provide endpoint to stream data
         */
    }
}
